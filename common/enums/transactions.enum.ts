export enum TransactionTypeEnum {
  INCOME = "INCOME",
  EXPENSE = "EXPENSE",
}

export enum TransactionFormTypeEnum {
  FAST = "Fast",
  COMPLETE = "Complete",
}

export enum TransactionStatusEnum {
  PENDING = "Pending",
  COMPLETED = "Completed",
}
